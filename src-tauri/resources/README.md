# Resources Directory

This directory contains bundled resources for TinyPix Pro.

## FFmpeg

Place FFmpeg binaries here for offline use:
- macOS: `ffmpeg` (static build)
- Windows: `ffmpeg.exe`, `ffprobe.exe`

If no FFmpeg binary is found here, the app will try to use the system-installed FFmpeg from PATH.
