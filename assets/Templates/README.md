# Built-in templates

Versioned TinyPix-owned presets belong here. Every template must declare a stable
ID, physical dimensions, pixel dimensions, DPI, output naming rule, and source.
The release build copies approved files to `Templates/` in the portable package.
