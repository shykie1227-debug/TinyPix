# Third-party licenses

Store the exact license text for every bundled engine, native library, model,
template source, and managed dependency here. Packaging fails when a required
license declared by the dependency baseline is missing.
