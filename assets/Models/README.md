# Offline model assets

Only reviewed offline model weights with fixed version, SHA-256, license, memory
budget, and commercial-use status may be placed here. Model binaries are release
inputs and are never downloaded at runtime.
