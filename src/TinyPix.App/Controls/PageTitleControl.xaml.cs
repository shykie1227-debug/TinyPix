using Microsoft.UI.Xaml.Controls;

namespace TinyPix.App.Controls;

/// <summary>
/// 24 Header/PageTitle — page heading (H1) + subtitle + optional help command.
/// </summary>
public sealed partial class PageTitleControl : UserControl
{
    public PageTitleControl()
    {
        InitializeComponent();
    }
}
