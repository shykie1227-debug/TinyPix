using Microsoft.UI.Xaml.Controls;

namespace TinyPix.App.Controls;

/// <summary>
/// 14 Card/Preset — reusable preset card template used inside a GridView.
/// </summary>
public sealed partial class PresetCardItem : UserControl
{
    public PresetCardItem()
    {
        InitializeComponent();
    }
}
