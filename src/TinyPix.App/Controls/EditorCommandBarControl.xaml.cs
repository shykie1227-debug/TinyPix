using Microsoft.UI.Xaml.Controls;

namespace TinyPix.App.Controls;

/// <summary>
/// 25 CommandBar/Editor — editor command bar with keyboard accelerators.
/// </summary>
public sealed partial class EditorCommandBarControl : UserControl
{
    public EditorCommandBarControl()
    {
        InitializeComponent();
    }
}
