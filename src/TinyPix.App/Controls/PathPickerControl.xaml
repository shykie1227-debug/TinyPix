<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="TinyPix.App.Controls.PathPickerControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!-- 9 Input/PathPicker: read-only path box + browse button. The path box
         supports Ctrl+C; the button activates via Enter/Space. A validation
         error surfaces with icon + text + focus, never color alone. -->
    <Grid ColumnSpacing="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox
            x:Name="PathBox"
            Grid.Row="0"
            Grid.Column="0"
            IsReadOnly="True"
            Header="{x:Bind Header, Mode=OneWay}"
            Text="{x:Bind Path, Mode=OneWay}"
            PlaceholderText="未选择"
            AutomationProperties.Name="输出路径" />
        <Button
            x:Name="BrowseButton"
            Grid.Row="0"
            Grid.Column="1"
            VerticalAlignment="Bottom"
            Content="浏览"
            Command="{x:Bind BrowseCommand}"
            Style="{ThemeResource TinyPixSecondaryButtonStyle}"
            AutomationProperties.Name="浏览输出目录" />

        <InfoBar
            x:Name="PathValidationBar"
            Grid.Row="1"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Severity="Error"
            IsOpen="{x:Bind HasValidationError, Mode=OneWay}"
            Message="{x:Bind ValidationMessage, Mode=OneWay}"
            IsClosable="False"
            AutomationProperties.Name="路径校验错误" />
    </Grid>
</UserControl>
