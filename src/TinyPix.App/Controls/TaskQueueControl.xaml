<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="TinyPix.App.Controls.TaskQueueControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TinyPix.App.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!-- 22 Shell/TaskQueue: title/count + job list + batch command bar.
         F6 in/out; Ctrl+J expands and focuses; queue rows reuse Card/Task. -->
    <Grid Padding="8" RowSpacing="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Spacing="6">
                <TextBlock
                    Text="任务队列"
                    Style="{ThemeResource BodyStrongTextBlockStyle}"
                    AutomationProperties.Name="任务队列" />
                <TextBlock
                    Text="{Binding ActiveCount}"
                    VerticalAlignment="Center"
                    Foreground="{ThemeResource TinyPixMutedBrush}" />
            </StackPanel>
            <CommandBar
                Grid.Column="1"
                HorizontalAlignment="Right"
                DefaultLabelPosition="Right"
                AutomationProperties.Name="任务队列批量操作">
                <AppBarButton
                    Label="全部取消"
                    Command="{Binding CancelAllCommand}"
                    AutomationProperties.Name="全部取消">
                    <AppBarButton.Icon><FontIcon Glyph="&#xE711;" /></AppBarButton.Icon>
                </AppBarButton>
                <AppBarButton
                    Label="清除已完成"
                    Command="{Binding ClearCompletedCommand}"
                    AutomationProperties.Name="清除已完成">
                    <AppBarButton.Icon><FontIcon Glyph="&#xE894;" /></AppBarButton.Icon>
                </AppBarButton>
            </CommandBar>
        </Grid>

        <ListView
            x:Name="JobList"
            Grid.Row="1"
            ItemsSource="{Binding Jobs}"
            AutomationProperties.Name="任务队列列表">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <local:TaskCardItem />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
